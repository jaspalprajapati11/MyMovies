import { relativeFontSize, lineHeight } from './Fonts'
import { relativeHeight, relativeWidth } from './Dimensions'



export default {
    relativeHeight,
    relativeWidth,
    // relativeFontSize,
    // lineHeight
}